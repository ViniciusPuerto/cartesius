# Caddy configuration for Cartesius API (HTTP-only)
# This setup listens on port 80 for any Host header, then proxies to the
# internal Rails container running on app:3000.
#
# When you later attach a real domain and want HTTPS, replace the site
# address (:80) with the domain name and open port 443 in your compose file.

http://52.67.23.66, http://ec2-52-67-23-66.sa-east-1.compute.amazonaws.com {
    encode gzip

    # Proxy all incoming requests to the Rails app service
    reverse_proxy app:3000
}
